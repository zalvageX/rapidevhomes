module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['var(--font-montserrat)', 'sans-serif'], // Montserrat as default
        mono: ['var(--font-geist-mono)', 'monospace'],  // Geist Mono if you want to keep it
      },
    },
  },
}
